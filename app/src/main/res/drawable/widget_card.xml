<?xml version="1.0" encoding="utf-8"?>
<!--
  A piece of Folio's paper, laid on the home screen. The hairline is what stops a
  near-white card from dissolving into a pale wallpaper, and it is the same border
  token every card inside the app carries.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/widget_bg" />
    <stroke android:width="1dp" android:color="@color/widget_border" />
    <corners android:radius="@dimen/widget_corner_radius" />
</shape>
