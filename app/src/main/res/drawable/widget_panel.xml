<?xml version="1.0" encoding="utf-8"?>
<!--
  The inner panel the empty invitation sits on — the app's bgAlt on the card's bg,
  the same relationship every card inside Folio has with the page under it.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/widget_bg_alt" />
    <corners android:radius="12dp" />
</shape>
