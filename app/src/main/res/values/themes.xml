<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      A bare platform theme. Folio's own colours come from FolioTheme in Compose;
      this exists only so the window has sane defaults before composition starts.
    -->
    <style name="Theme.Folio" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
</resources>
